# The app runs with built-in demo data; no env vars required for local dev or deploy.
#
# Optional — only if you wire Supabase later (see src/lib/supabase/):
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
