# AidBridge runs with built-in demo data. No environment variables are required.
#
# If `npm run dev` shows missing chunk errors or EMFILE on macOS, run in the same terminal first:
#   ulimit -n 10240
# Or use a production-style local server (no dev watcher):
#   npm run preview
